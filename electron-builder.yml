appId: com.paulomanrique.streamer-copilot
productName: Streamer Copilot
directories:
  output: release
files:
  - dist/**/*
  - package.json
extraResources:
  - from: build/icon.png
    to: icon.png
asar: true
compression: maximum
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
publish:
  provider: github
  owner: paulomanrique
  repo: streamer-copilot
mac:
  icon: build/icon.icns
  category: public.app-category.utilities
  target:
    - dmg
    - zip
win:
  icon: build/icon.ico
  target:
    - nsis
linux:
  icon: build/icon.png
  maintainer: Paulo Manrique <paulo@manriq.dev>
  target:
    - AppImage
    - deb
